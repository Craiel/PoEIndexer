#:kivy 1.0


BoxLayout:
    orientation: 'vertical'

    canvas.before:
        Color:
            rgb: .6, .6, .6
        Rectangle:
            size: self.size
            source: 'data/background.png'

    ActionBar:
        id: actionBar

    ScrollView:
        id: scrollView
        size_hint_y: None
        height: 0